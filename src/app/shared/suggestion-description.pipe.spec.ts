/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { SuggestionDescriptionPipe } from './suggestion-description.pipe';

describe('Pipe: SuggestionDescriptione', () => {
  it('create an instance', () => {
    let pipe = new SuggestionDescriptionPipe();
    expect(pipe).toBeTruthy();
  });
});
